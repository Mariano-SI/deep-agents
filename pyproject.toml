[project]
name = "2-introduction-to-deep-agents"
version = "0.1.0"
description = ""
authors = [
    {name = "Mariano-SI",email = "mariano.silva@ufvjm.edu.br"}
]
requires-python = ">=3.11,<4.0"
dependencies = [
    "deepagents (>=0.7.8,<0.8.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "langchain (>=1.3.16,<2.0.0)",
    "langchain-anthropic (>=1.6.1,<2.0.0)",
    "langchain-openai (>=1.6.0,<2.0.0)",
    "langchain-community (>=0.4.2,<0.5.0)",
    "langchain-mcp-adapters (>=0.3.2,<0.4.0)",
    "langgraph (>=1.2.11,<2.0.0)"
]

[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
